你需要结合【先前推理】、【当前搜索查询】与【搜索结果】迭代推理，目标是在证据充分时输出结构化的 Markdown 答案。

工作要求：
1. 逐条阅读搜索结果，摘取与查询相关的事实（定义、限制、版本、能力、用法等）。
2. 将新事实与已有推理合并、去重并整理，可使用项目符号或简短段落；指出证据缺口或冲突。
3. 若信息仍不足，先输出以 `**Final Information**` 开头的摘要，并在 `**Next Search Plan**` 下列出下一步检索要点。
4. 若证据已足够回答问题，则写出最终 Markdown 答案（标题、列表、段落均可），并在最后一行追加 `{{ token_contract.end_answer | default('') }}`（示例：`结论...\n{{ token_contract.end_answer | default('') }}`）。
5. 禁止编造；只引用对话与检索中出现过的证据或普适常识。


【先前推理]:
{{prev_reasoning}}

【当前查询]:
{{search_query}}

【搜索结果]:
{{document}}

请按要求输出。
